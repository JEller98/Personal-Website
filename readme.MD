# To-do:
1) Test out Tablet/Mobile view on real hardware*
2) Allow project cards to be selectable via ``Tab`` key; lock users' ``Tab`` inputs inside the modal until they select the close button.
3) Research + implement ARIA attributes
4) Display 2 rows' worth of projects and hide the rest under a "View More..." interactable.
5) Isolate data into a separate JSON file instead of keeping it at the top of ``script.js``.

*Mobile view could have smaller text
